# Versioned, non-secret policy. Copy the root path to the untracked local .env.
artifact_root: "../runtime"
required_directories:
  - model_weights
  - captures
  - cache
  - runs
  - exports

# If WSL2 is used, map this root to the corresponding mounted path locally.
